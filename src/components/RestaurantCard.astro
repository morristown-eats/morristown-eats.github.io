---
interface Props {
  name: string;
  slug: string;
  cuisine: string[];
  neighborhood: string;
  price: string;
  byob?: boolean;
  ownership?: string;
  one_liner: string;
  image?: string;
  image_alt?: string;
  visit_status?: string;
  link?: boolean;
}

const {
  name,
  slug,
  cuisine,
  neighborhood,
  price,
  byob = false,
  ownership = 'independent',
  one_liner,
  image,
  image_alt,
  visit_status,
  link = true,
} = Astro.props;

// Detail pages are archived — pages like /the-40 render the card unlinked.
const Wrapper = link ? 'a' : 'div';

const neighborhoodDisplay = neighborhood
  .split('-')
  .map((w: string) => w.charAt(0).toUpperCase() + w.slice(1))
  .join(' ');

const cuisineDisplay = cuisine
  .map((c: string) => c.split('-').map((w: string) => w.charAt(0).toUpperCase() + w.slice(1)).join(' '))
  .join(' · ');
---

<article class="restaurant-card">
  <Wrapper href={link ? `/restaurants/${slug}` : undefined}>
    <div class="card-image">
      {image ? (
        <img
          src={image}
          alt={image_alt || `${name} — ${neighborhoodDisplay}`}
          loading="lazy"
          decoding="async"
        />
      ) : (
        <div class="card-image-placeholder"><span>{neighborhoodDisplay}</span></div>
      )}
    </div>
    <div class="card-body">
      <div class="card-meta">
        <span>{neighborhoodDisplay}</span>
        <span class="card-meta-sep">·</span>
        <span class="price">{price}</span>
        {byob && (
          <>
            <span class="card-meta-sep">·</span>
            <span>BYOB</span>
          </>
        )}
      </div>
      <h3 class="card-name">{name}</h3>
      <p class="card-cuisine">{cuisineDisplay}</p>
      <p class="card-hook">"{one_liner}"</p>
      <div class="card-tags">
        {ownership === 'local_group' && (
          <span class="tag badge-local-group">Local group</span>
        )}
        {ownership === 'regional_chain' && (
          <span class="tag badge-local-group">Regional</span>
        )}
        {visit_status === 'fact-shell' && (
          <span class="tag" style="font-style:italic; opacity:0.65;">Unvisited</span>
        )}
      </div>
    </div>
  </Wrapper>
</article>
